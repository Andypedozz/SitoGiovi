---
const projects = [
    { id: "villa-moderna", name: "Villa Moderna" },
    { id: "loft-industriale", name: "Loft Industriale" },
    { id: "residenza-lago", name: "Residenza sul Lago" },
];

const photos = [
    {
        id: 1,
        title: "Render esterno",
        description: "Vista crepuscolare della facciata principale.",
        project: "Villa Moderna",
        imageUrl: "https://images.unsplash.com/photo-1493809842364-78817add7ffb?auto=format&fit=crop&w=600&q=80",
    },
    {
        id: 2,
        title: "Living minimale",
        description: "Zona giorno con arredi su misura e luce naturale.",
        project: "Loft Industriale",
        imageUrl: "https://images.unsplash.com/photo-1484154218962-a197022b5858?auto=format&fit=crop&w=600&q=80",
    },
    {
        id: 3,
        title: "Dettaglio terrazza",
        description: "Outdoor con elementi in cemento e legno.",
        project: "Residenza sul Lago",
        imageUrl: "https://images.unsplash.com/photo-1505691938895-1758d7feb511?auto=format&fit=crop&w=600&q=80",
    },
];
---

<div
    id="screen-foto"
    class="screen hidden bg-gray-50 text-gray-900 rounded-2xl border border-gray-200 shadow-xl p-8 space-y-8"
>
    <header class="space-y-2 border-b border-gray-200 pb-4">
        <p class="text-sm uppercase tracking-[0.35em] text-gray-500">Galleria</p>
        <h2 class="text-3xl font-bold text-gray-900">Gestione Foto</h2>
        <p class="text-gray-500 max-w-3xl">
            Carica nuove immagini, collega ogni foto al progetto corretto e mantieni aggiornata la libreria
            visuale. Tutti i campi sono obbligatori per garantire contenuti coerenti e completi.
        </p>
    </header>

    <div class="grid grid-cols-1 lg:grid-cols-2 gap-10">
        <!-- Form di caricamento -->
        <section class="bg-white border border-gray-200 rounded-2xl shadow-lg p-6 space-y-6">
            <div>
                <h3 class="text-xl font-semibold text-gray-900">Aggiungi una nuova foto</h3>
                <p class="text-gray-500 text-sm">Compila i campi per creare un record pronto alla pubblicazione.</p>
            </div>

            <form action="#" class="space-y-5">
                <div>
                    <label for="photo-title" class="block text-sm font-medium text-gray-600">Titolo</label>
                    <input
                        id="photo-title"
                        type="text"
                        placeholder="Esempio: Render cucina"
                        class="mt-1 w-full rounded-lg border border-gray-300 px-4 py-2 text-gray-900 focus:border-gray-800 focus:ring-2 focus:ring-gray-800/30"
                    />
                </div>

                <div>
                    <label for="photo-description" class="block text-sm font-medium text-gray-600">Descrizione</label>
                    <textarea
                        id="photo-description"
                        rows="4"
                        placeholder="Descrivi il contenuto, l'atmosfera o la tecnica utilizzata..."
                        class="mt-1 w-full rounded-lg border border-gray-300 px-4 py-2 text-gray-900 focus:border-gray-800 focus:ring-2 focus:ring-gray-800/30 resize-none"
                    ></textarea>
                </div>

                <div>
                    <label for="photo-project" class="block text-sm font-medium text-gray-600">Progetto di riferimento</label>
                    <select
                        id="photo-project"
                        class="mt-1 w-full rounded-lg border border-gray-300 px-4 py-2 text-gray-900 bg-white focus:border-gray-800 focus:ring-2 focus:ring-gray-800/30"
                    >
                        <option value="" selected disabled>Scegli un progetto</option>
                        {projects.map((project) => (
                            <option value={project.id}>{project.name}</option>
                        ))}
                    </select>
                </div>

                <div>
                    <label for="photo-url" class="block text-sm font-medium text-gray-600">Link immagine</label>
                    <input
                        id="photo-url"
                        type="url"
                        placeholder="https://..."
                        class="mt-1 w-full rounded-lg border border-gray-300 px-4 py-2 text-gray-900 focus:border-gray-800 focus:ring-2 focus:ring-gray-800/30"
                    />
                    <p class="mt-1 text-xs text-gray-500">Utilizza un URL pubblico o carica il file nella tua CDN.</p>
                </div>

                <div class="flex flex-wrap gap-3 pt-4">
                    <button
                        type="submit"
                        class="inline-flex items-center justify-center rounded-lg bg-gray-900 px-4 py-2 text-sm font-semibold text-white shadow-sm transition hover:bg-black"
                    >
                        Salva foto
                    </button>
                    <button
                        type="button"
                        class="inline-flex items-center justify-center rounded-lg border border-gray-300 px-4 py-2 text-sm font-semibold text-gray-700 transition hover:border-gray-900 hover:text-gray-900"
                    >
                        Pulisci campi
                    </button>
                </div>
            </form>
        </section>

        <!-- Libreria e anteprima -->
        <section class="space-y-6">
            <div class="bg-white border border-gray-200 rounded-2xl shadow-lg p-6 space-y-4">
                <div class="flex items-center justify-between">
                    <div>
                        <h3 class="text-xl font-semibold text-gray-900">Stato caricamento</h3>
                        <p class="text-sm text-gray-500">Controlla come apparirà la scheda prima della pubblicazione.</p>
                    </div>
                    <span class="rounded-full bg-gray-100 px-3 py-1 text-xs font-semibold text-gray-600">Bozza</span>
                </div>

                <div class="rounded-xl border border-dashed border-gray-300 bg-gray-50 p-4 text-center">
                    <p class="text-sm text-gray-500">
                        Incolla il link dell'immagine per vedere un'anteprima live oppure trascina il file nella tua piattaforma di
                        hosting.
                    </p>
                </div>

                <ul class="space-y-3 text-sm text-gray-600">
                    <li class="flex items-start gap-2">
                        <span class="mt-1 h-2 w-2 rounded-full bg-gray-400"></span>
                        Titolo, descrizione e progetto sono obbligatori per ogni elemento.
                    </li>
                    <li class="flex items-start gap-2">
                        <span class="mt-1 h-2 w-2 rounded-full bg-gray-400"></span>
                        Utilizza immagini con dimensione minima 1600px sul lato lungo per uniformità visiva.
                    </li>
                </ul>
            </div>

            <div class="bg-white border border-gray-200 rounded-2xl shadow-lg p-6 space-y-4">
                <div class="flex items-center justify-between">
                    <h3 class="text-xl font-semibold text-gray-900">Foto già presenti</h3>
                    <button class="text-sm font-medium text-gray-500 hover:text-gray-900">Vedi tutte</button>
                </div>

                <div class="grid grid-cols-1 sm:grid-cols-2 gap-4">
                    {photos.map((photo) => (
                        <article class="rounded-xl border border-gray-200 shadow-sm overflow-hidden">
                            <img src={photo.imageUrl} alt={photo.title} class="h-40 w-full object-cover" loading="lazy" />
                            <div class="p-4 space-y-1">
                                <p class="text-xs uppercase tracking-widest text-gray-500">{photo.project}</p>
                                <h4 class="text-lg font-semibold text-gray-900">{photo.title}</h4>
                                <p class="text-sm text-gray-600 leading-snug">{photo.description}</p>
                                <div class="flex gap-3 pt-3 text-xs font-medium text-gray-500">
                                    <button class="hover:text-gray-900">Modifica</button>
                                    <span aria-hidden="true">•</span>
                                    <button class="text-red-500 hover:text-red-700">Elimina</button>
                                </div>
                            </div>
                        </article>
                    ))}
                </div>
            </div>
        </section>
    </div>
</div>
